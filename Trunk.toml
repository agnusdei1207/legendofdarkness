[build]
target = "index.html"
dist = "dist"

[watch]
ignore = ["dist", "target"]

[serve]
address = "0.0.0.0"
port = 8080
open = false

# Proxy API requests to backend
[[serve.proxy]]
backend = "http://api:3000/api/"
rewrite = "/api/"
